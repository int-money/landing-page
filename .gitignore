node_modules
.next
.env
.env.local
*.tsbuildinfo

# Lock files (pnpm only — no npm or yarn)
package-lock.json
yarn.lock

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

